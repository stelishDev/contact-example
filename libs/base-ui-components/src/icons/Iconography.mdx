import { Meta, IconGallery, IconItem } from "@storybook/blocks";
import * as icons from "./index";

<Meta title="StyleGuide/Iconography" />

# Iconography

Here is a list of all the icons used in the Contact example application.  

## Icon Gallery&nbsp;  

<IconGallery>
  {Object.entries(icons).map(([name, Icon]) => (
    <IconItem name={name}>
      <div className="bg-gray-100">
        <Icon className="h-[128px] w-[128px] fill-current stroke-current bg-gray-100" />
      </div>
    </IconItem>
  ))}
</IconGallery>
