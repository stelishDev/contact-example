import { Meta, IconGallery, IconItem } from "@storybook/blocks";
import * as icons from "./index";

<Meta title="StyleGuide/Iconography" />

# Iconography

Here is a list of all the icons used in the HPSC GUI application.  

## Icon Gallery&nbsp;  

<IconGallery>
  {Object.entries(icons).map(([name, Icon]) => (
    <IconItem name={name} className="bg-slate-200">
        <Icon className="h-[128px] w-[128px] fill-current stroke-current" />
    </IconItem>
  ))}
</IconGallery>
